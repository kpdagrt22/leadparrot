LeadParrot weighted-score breakdown: relevance·0.35 + intent·0.30 + urgency·0.20 + fit·0.15. Mono labels with weights, hairline tracks.

```jsx
<ScoreBars relevance={88} intent={82} urgency={75} fit={70} />
```

Each value is 0–100. Use inside the lead-detail score panel.
